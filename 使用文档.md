# 云上重走长征路使用手册

本文说明当前仓库已经实现的学生端、管理端和开发调试流程。生产部署与环境变量配置请参阅 [部署文档](部署文档.md)。

## 1. 产品角色

- **学生用户**：通过微信小程序登录、绑定学生身份、同步微信运动步数、查看虚拟路线进度与学习内容。
- **活动维护人员**：通过管理端查看统计数据，导入和维护学生资料，并处理学生解绑。
- **项目贡献者**：维护 Java 微服务、Vue 3 管理端、原生微信小程序以及 Compose 部署配置。

## 2. 学生端流程

### 2.1 登录与身份绑定

1. 打开微信小程序并完成微信登录授权。
2. 按页面提示设置头像和昵称。
3. 输入学号和姓名，与管理员预先导入的学生资料进行匹配。
4. 绑定成功后，用户数据会关联到对应年级和班级。

登录接口会返回访问令牌与刷新令牌。访问令牌用于普通业务请求，刷新令牌只允许调用令牌刷新接口，不能直接访问学生或管理员 API。小程序会持久化刷新令牌；遇到访问令牌过期时，只发起一次并发刷新并重试原请求一次。

微信 `chooseAvatar` 返回的是小程序本地临时文件。当前版本会把它转存到小程序本地持久目录供本机展示，但不会把本地文件路径提交给后端；昵称仍会同步。若要让头像跨设备显示，需要后续接入受控对象存储上传流程并获得 HTTPS 地址。

### 2.2 同步微信运动数据

学生授权微信运动后，可在首页触发步数同步。服务端会解密微信返回的数据，并按日期幂等保存：同一天重复同步时，只增加比上次记录多出的有效步数，避免重复累计。

当前默认规则位于 `changzheng-sport/src/main/resources/application.yml`：

- `2,000` 步折算为 `1` 公里。
- 每日最多计入 `30,000` 个有效步数。
- 原始步数超过 `50,000` 时标记为异常，但有效步数仍按每日上限裁剪。
- 里程保留两位小数并向下取整。

这些值同时在小程序配置中提供默认展示值。修改规则时，应同步检查服务端和 `changzheng-miniprogram/miniprogram/config/index.js`，以免前后端显示不一致。

### 2.3 路线与学习内容

小程序通过个人进度接口展示累计步数、累计里程、当前节点和下一节点。达到节点要求后，可以查看对应的文章、图片、音频或视频内容，并提交已学习状态。内容列表、详情和学习记录都会在服务端验证节点已解锁；文章当前按纯文本展示，不执行管理员输入的 HTML。

### 2.4 排行榜

当前服务端公开以下排行榜能力：

- 总榜：全部已参与用户的个人里程排行。
- 年级榜：当前用户所在年级的个人里程排行。
- 我的排名：当前用户的个人名次与基本班级信息。

当前版本没有公开班级汇总排行榜接口。未绑定学生身份的用户也无法获得年级维度的数据。

## 3. 管理端流程

管理端部署在 `/admin/`，通过独立的管理员令牌访问 `/api/admin/**` 和受保护的上传接口。当前服务端已经实现：

- 管理员登录与当前管理员信息查询。
- 仪表盘统计数据查询。
- 学生列表、筛选和统计。
- Excel 学生数据导入。
- 学生资料更新、停用和解绑；停用采用软删除，已绑定记录必须先解绑。
- 路线节点创建、查询、更新和禁用。
- 节点学习内容创建、更新和软删除。
- 受控图片、音频和视频上传。

初次部署时，只有数据库中不存在启用中的管理员，`ADMIN_USERNAME` 和 `ADMIN_PASSWORD` 才会用于创建初始 `SUPER_ADMIN`。管理员登录在 15 分钟内最多失败 10 次；Redis 不可用时登录会安全失败。Excel 导入仅接受 `.xls`/`.xlsx`，限制 5 MB 和 5,000 行。上线后应妥善保管管理员凭证，不要在截图、Issue 或日志中暴露令牌和学生数据。

## 4. 当前实现边界

当前边界如下：

- `/api/common/banners` 已存在，但当前返回空列表，尚无后台轮播内容模型。
- 成就页面根据进度数据在小程序本地计算展示，尚无独立服务端成就判定与持久化 API。
- 当前提供个人总榜、年级个人榜和“我的排名”，不提供班级汇总榜。
- 学生导入模板由受管理员认证保护的后端接口生成 `.xlsx`，字段与导入解析保持一致。
- 已绑定学生的姓名、专业、班级和年级不能直接修改，以避免认证底表与用户资料不一致；需要变更时应先解绑。
- 节点与内容删除采用禁用或软删除；上传文件不会因为内容停用而自动删除。

## 5. 开发与验证

### 5.1 后端和管理端

完整源码验证命令：

```bash
./mvnw --batch-mode --no-transfer-progress clean verify
npm --prefix changzheng-admin-web ci --ignore-scripts
npm --prefix changzheng-admin-web run build
npm --prefix changzheng-admin-web audit --audit-level=high
docker compose --env-file .env.example config --quiet
```

管理端开发服务器：

```bash
npm --prefix changzheng-admin-web ci --ignore-scripts
npm --prefix changzheng-admin-web run dev
```

开发服务器默认将 `/api` 代理到 `http://localhost:8080`。设置 `VITE_USE_MOCK=true` 才会启用管理端模拟登录；未设置时连接真实后端。

### 5.2 微信小程序

使用微信开发者工具导入 `changzheng-miniprogram` 目录。环境配置位于 `changzheng-miniprogram/miniprogram/config/index.js`：

- 仓库默认 `currentEnv = 'prod'`，使用生产地址并关闭 Mock。
- 本地界面开发时可显式改为 `currentEnv = 'dev'`，使用开发地址和 Mock 数据。
- 首次运行真实接口前必须将生产 `baseUrl` 改为实际 HTTPS 域名，并在微信公众平台配置合法请求域名；保留的 `.invalid` 占位域名不会连接真实服务。

Mock 数据仅用于界面开发，不能用于验证真实登录、微信运动解密、JWT 权限或数据持久化。

## 6. 常见问题

### 认证时提示“学号不存在”

确认管理员已经导入对应学生记录，并检查学号和姓名是否完全一致。不要通过公开 Issue 提交真实学号或学生名单。

### 微信运动步数没有更新

先确认用户已经授予微信运动权限，并在微信运动中完成数据刷新。重复同步不会重复累计；只有比当日已有记录更高的有效步数会产生新增里程。

### 管理端接口返回 401 或 403

确认登录令牌未过期且令牌角色为 `ADMIN` 或 `SUPER_ADMIN`。学生令牌、refresh token 以及客户端伪造的 `X-Admin-Id` 都不能访问管理员接口。

### 轮播图为空或成就没有服务端记录

这是当前实现边界，不代表请求失败。轮播接口暂时返回空列表，成就卡片目前由客户端基于进度计算；计划见 [ROADMAP.md](ROADMAP.md)。

## 7. 安全与许可证

漏洞报告方式见 [SECURITY.md](SECURITY.md)，代码贡献流程见 [CONTRIBUTING.md](CONTRIBUTING.md)。本项目采用 [Apache License 2.0](LICENSE)，版权声明见 [NOTICE](NOTICE)。
