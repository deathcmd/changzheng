# 云上重走长征路部署指南

本项目以 Docker Compose 作为受支持的部署方式。部署前需要 JDK 17、Maven 3.8+、Node.js 20.19+（或 22.12+）、npm 和带 Compose 插件的 Docker。

## 1. 准备配置

    cp .env.example .env

逐项替换 .env 中的占位值。不要复用数据库、Redis、JWT、AES 和管理员密码，也不要把 .env 提交到 Git。JWT Secret 至少 32 字节；AES Key 必须为 16、24 或 32 字节；管理员初始密码至少 12 个字符。

CORS_ALLOWED_ORIGIN 填写管理端准确 Origin，例如 https://admin.example.org。FILE_UPLOAD_URL 填写对外媒体地址，例如 https://app.example.org/uploads。

## 2. 验证和启动

    docker compose --env-file .env config --quiet
    ./deploy.sh

部署脚本会执行后端测试与打包、npm ci、管理端构建、镜像构建和 Compose 启动。它不会安装系统软件、创建 root systemd 服务或把 Secret 写进服务文件。

查看状态和日志：

    docker compose --env-file .env ps
    docker compose --env-file .env logs --tail=200 gateway

默认只有 Nginx 的 80 端口对宿主机开放。数据库、中间件和各微服务不应直接暴露到公网。生产环境应由外部反向代理或负载均衡器提供 HTTPS、证书续期和访问控制，再转发到 Nginx。

## 3. 数据库

首次创建 MySQL 数据卷时，镜像会按文件名顺序运行 sql/ 中的脚本。管理员不再使用公开默认密码；当没有启用中的管理员时，ADMIN_USERNAME 和 ADMIN_PASSWORD 会创建初始 SUPER_ADMIN。

已有数据库不会自动重新执行初始化脚本。升级前先做经验证的备份，再审核并执行新增迁移：

    mysql --user=changzheng --password changzheng_db < sql/V3__security_hardening.sql

不要把密码直接写在命令行参数中；上面的 --password 会交互式提示。

## 4. 管理端与小程序

管理端位于 /admin/，API 位于 /api/，上传媒体位于 /uploads/。开发服务器通过网关 8080 联调；只有显式设置 VITE_USE_MOCK=true 才启用模拟管理员登录。

微信小程序生产配置在 changzheng-miniprogram/miniprogram/config/index.js。发布前确认 currentEnv 为 prod、useMock 为 false，并在微信公众平台登记 HTTPS API 域名。

## 5. 更新与回滚

更新前记录当前 Git commit，备份 MySQL 与上传卷，并先在非生产环境运行：

    mvn --batch-mode verify
    npm --prefix changzheng-admin-web ci
    npm --prefix changzheng-admin-web run build
    docker compose --env-file .env config --quiet

数据库迁移通常不可通过切换镜像自动回滚。发生故障时，应恢复已验证的数据库/上传备份并部署先前记录的 commit，而不是删除整个 Compose 数据卷。
