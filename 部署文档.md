# 云上重走长征路部署指南

Docker Compose 是当前仓库支持的集成部署方式。本指南默认在 Linux、macOS、WSL 或 Git Bash 中执行命令；生产环境应使用独立的域名、HTTPS 入口、备份策略和密钥管理方案。

## 1. 环境要求

- JDK 17
- Maven 3.8+
- Node.js 20.19+ 或 22.12+
- npm
- Docker 与 Docker Compose 插件
- Bash

部署脚本会在宿主机运行 Maven 和 npm，再使用 Docker 构建镜像，因此这些工具都必须在执行 `deploy.sh` 的环境中可用。

## 2. 准备环境变量

在仓库根目录复制模板：

```bash
cp .env.example .env
```

PowerShell 可使用：

```powershell
Copy-Item .env.example .env
```

逐项替换 `.env` 中的占位值：

| 变量 | 要求 |
| --- | --- |
| `MYSQL_ROOT_PASSWORD` | MySQL root 随机强密码 |
| `MYSQL_APP_PASSWORD` | 应用数据库用户密码，不与 root 密码复用 |
| `REDIS_PASSWORD` | Redis 随机强密码 |
| `JWT_SECRET` | 至少 32 个随机 ASCII 字节 |
| `AES_KEY` | 恰好 16、24 或 32 个 ASCII 字节；更换会影响既有加密数据读取 |
| `WX_APPID` | 微信小程序 AppID |
| `WX_SECRET` | 微信小程序 AppSecret |
| `ADMIN_USERNAME` | 首个管理员用户名 |
| `ADMIN_PASSWORD` | 至少 12 个字符的随机强密码 |
| `CORS_ALLOWED_ORIGIN` | 管理端准确 Origin，例如 `https://admin.example.org`，不包含路径或通配符 |
| `FILE_UPLOAD_URL` | 对外媒体根地址，例如 `https://app.example.org/uploads` |

不要复用数据库、Redis、JWT、AES 和管理员凭证，也不要把 `.env`、微信凭证、私钥、学生数据或数据库导出提交到 Git。

## 3. 验证并启动

先验证 Compose 配置：

```bash
docker compose --env-file .env config --quiet
```

确认没有占位值后执行：

```bash
./deploy.sh
```

部署脚本依次执行：

1. 检查 Docker、Compose、Maven、npm 和 `.env`。
2. 校验 Compose 配置。
3. 执行 Maven 测试与打包。
4. 使用 `npm ci` 安装锁定依赖并构建管理端。
5. 拉取基础镜像、构建服务镜像并启动 Compose。

脚本不会安装系统软件、创建 root systemd 服务，也不会把 Secret 写入服务文件。

## 4. 部署后检查

查看服务状态：

```bash
docker compose --env-file .env ps
```

查看网关或其他服务日志：

```bash
docker compose --env-file .env logs --tail=200 gateway
docker compose --env-file .env logs --tail=200 auth-service
```

主要访问入口：

- `/admin/`：Vue 管理端
- `/api/`：小程序和管理端 API
- `/uploads/`：上传媒体读取路径

默认只有 Nginx 的 `80` 端口对宿主机开放。MySQL、Redis、Nacos、RocketMQ 和各微服务只位于 Compose 网络，不应直接暴露到公网。生产环境应由外部反向代理或负载均衡器提供 HTTPS、证书续期、请求大小限制和访问日志，再将流量转发到 Nginx。

## 5. 数据库初始化与迁移

首次创建 MySQL 数据卷时，镜像会按文件名顺序执行 `sql/` 中的脚本。只有数据库中不存在启用中的管理员时，`ADMIN_USERNAME` 和 `ADMIN_PASSWORD` 才会创建初始 `SUPER_ADMIN`。

已有数据库不会自动重新执行初始化脚本。升级前应先创建并验证数据库备份，再由维护人员审核尚未应用的迁移。以当前安全迁移为例，可从仓库根目录执行：

```bash
docker compose --env-file .env exec -T mysql \
  sh -c 'MYSQL_PWD="$MYSQL_PASSWORD" exec mysql --user="$MYSQL_USER" "$MYSQL_DATABASE"' \
  < sql/V3__security_hardening.sql
```

不要通过删除 Compose 数据卷来“重跑”迁移。数据库迁移通常无法通过切换旧镜像自动回滚。

## 6. 管理端与微信小程序

管理端开发服务器默认将 `/api` 代理到 `http://localhost:8080`。只有显式设置 `VITE_USE_MOCK=true` 才启用模拟管理员登录；生产构建应连接真实后端。

微信小程序配置位于 `changzheng-miniprogram/miniprogram/config/index.js`。发布前必须：

1. 将生产 `baseUrl` 改为实际 HTTPS 域名。
2. 确认 `currentEnv` 为 `prod`、`useMock` 为 `false`。
3. 在微信公众平台登记合法请求域名。
4. 使用与 `.env` 中 `WX_APPID` 对应的小程序项目。
5. 核对 [使用手册的当前实现边界](使用文档.md#4-当前实现边界)。

## 7. 更新与回滚

更新前记录当前 Git commit，备份 MySQL 与上传卷，并先在非生产环境运行：

```bash
mvn --batch-mode verify
npm --prefix changzheng-admin-web ci
npm --prefix changzheng-admin-web run build
docker compose --env-file .env config --quiet
```

更新时应审查 `.env.example`、Compose、SQL 迁移和依赖锁文件的变化。发生故障时，恢复经过验证的数据库和上传卷备份，再部署先前记录的 commit；不要在没有备份的情况下删除数据卷或强行逆向执行 SQL。

## 8. 安全与许可证

安全问题请按照 [SECURITY.md](SECURITY.md) 私下报告。部署和分发本项目时应保留 [Apache License 2.0](LICENSE) 以及 [NOTICE](NOTICE) 中的版权声明。
